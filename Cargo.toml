[package]
name = "after-class"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "=0.19", features = ["debug", "dynamic_linking", "free_camera"] }
rand = "0.10.2"
strum = { version = "0.28.0", features = ["derive"] }
